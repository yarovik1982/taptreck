version: '3.9'

services:
  app:
    image: beer-front-app
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: taptrack
    ports:
      - "443:443"
    environment:
      - "SERVER_PORT=443"
    restart: always